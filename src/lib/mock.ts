import type {
  LogEvent,
  Tag,
  KnowledgeObject,
  Integration,
  ActivityEntry,
  CorrelationRule,
  Playbook,
  IncidentCase,
  ScheduledReport,
  Hunt,
} from "./types";

export const seedTags: Tag[] = [];

export const seedLogs: LogEvent[] = [];

export const seedKnowledgeObjects: KnowledgeObject[] = [];

export const seedIntegrations: Integration[] = [];

export const seedActivity: ActivityEntry[] = [];

export const seedCorrelationRules: CorrelationRule[] = [];

export const seedPlaybooks: Playbook[] = [];

export const seedCases: IncidentCase[] = [];

export const seedReports: ScheduledReport[] = [];

export const seedHunts: Hunt[] = [];